name: Deploy Website

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Build site
        run: npm run build
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}

      - name: Debug build output
        run: |
          echo "=== Complete dist folder contents ==="
          find dist/ -type f | sort
          echo ""
          echo "=== index.html content ==="
          cat dist/index.html
          echo ""
          echo "=== Checking for main.tsx references ==="
          if grep -q "main.tsx" dist/index.html; then
            echo "❌ PROBLEM: index.html still references main.tsx"
            echo "This means Vite isn't replacing the script tag properly"
          else
            echo "✅ Good: index.html does NOT reference main.tsx"
          fi
          echo ""
          echo "=== JavaScript files in dist ==="
          find dist/ -name "*.js" | head -10

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          force_orphan: true
