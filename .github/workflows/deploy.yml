      - name: Verify OneSignal in Production Build
        run: |
          echo "🔍 Verifying OneSignal in production build..."
          
          # Check if App ID was injected
          if grep -q "3417d840-c226-40ba-92d6-a7590c31eef3" dist/index.html; then
            echo "✅ Hardcoded App ID found (using fallback)"
          else
            echo "✅ App ID was replaced"
          fi
          
          # Check for OneSignal script
          echo ""
          echo "📋 OneSignal script check:"
          grep -c "OneSignalSDK.page.js" dist/index.html || echo "Not found"
          
          # Check service worker path
          echo ""
          echo "🔧 Service Worker configuration:"
          grep -A2 -B2 "serviceWorker" dist/index.html || echo "Not found"
          
          echo ""
          echo "✅ Build verification complete"
